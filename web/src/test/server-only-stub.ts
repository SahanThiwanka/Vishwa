/**
 * Test stub for the `server-only` package.
 *
 * See vitest.config.mts for why this exists. Importing this file has no effect,
 * which is the point: it lets server modules be unit-tested without the
 * bundler-level guard rejecting them.
 */
export {};
